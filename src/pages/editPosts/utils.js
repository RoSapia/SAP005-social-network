import { onNavigate } from '../../utils/history.js';
import { EditPosts } from './index.js';

export const returnMyPosts = (response) => {
       
          onNavigate('/myPosts');
        
      }
